name: CI - Symbolic Verification

on:
  push:
    branches: [ "main" ] 
  workflow_dispatch: 

jobs:
  symbolic-proofs:
    runs-on: [self-hosted, arm64, radxa]
    steps:
      - uses: actions/checkout@v4

      - name: Check and Install System Dependencies
        run: |
          if ! command -v javac &> /dev/null || ! command -v z3 &> /dev/null || ! command -v cvc4 &> /dev/null; then
            echo "Dependencies missing, installing jdk, z3, cvc4 and sby runtime deps..."
            sudo apt-get update
            sudo apt-get install -y default-jdk z3 cvc4 python3-click python3-yaml
          else
            echo "System dependencies already installed."
          fi

      - name: Check and install Yosys (build from source if too old)
        run: |
          set -e
          export PATH="$HOME/.local/bin:$PATH"
          # SBY v0.68's prep flow runs `formalff ... -hierarchy`, an option added
          # in Yosys 0.24. Debian bookworm ships yosys 0.23, which fails with an
          # opaque `SymbiYosys failure`. Require >= 0.33 (the tested combo, same
          # as the WSL dev setup): otherwise build yosys-0.33 from source, once.
          # The self-hosted runner keeps $HOME/.local, so later runs skip this.
          # Build in $HOME, not /tmp: on the radxa, /tmp can be read-only.
          YOSYS_VER=$(yosys --version 2>/dev/null | sed -n 's/^Yosys \([0-9][0-9]*\.[0-9][0-9]*\).*/\1/p')
          NEED="0.33"
          if [ -n "$YOSYS_VER" ] && [ "$(printf '%s\n%s\n' "$YOSYS_VER" "$NEED" | sort -V | tail -1)" = "$YOSYS_VER" ]; then
            echo "Yosys OK: $(yosys --version)"
          else
            echo "Yosys ${YOSYS_VER:-<none>} too old for SBY v0.68, building yosys-0.33 from source..."
            sudo apt-get update
            sudo apt-get install -y build-essential clang bison flex libreadline-dev tcl-dev libffi-dev git gawk pkg-config python3 zlib1g-dev
            export TMPDIR="$HOME/tmp"
            mkdir -p "$TMPDIR"
            BUILD_DIR="$HOME/yosys-build"
            rm -rf "$BUILD_DIR"
            git clone --depth 1 --branch yosys-0.33 https://github.com/YosysHQ/yosys.git "$BUILD_DIR"
            make -C "$BUILD_DIR" config-gcc
            # ABC is not used by the sby smtbmc/Z3 formal flow (only synth_*);
            # building it from source takes ~20 min and heats up the passively
            # cooled radxa. Keep it disabled for this build.
            echo 'ENABLE_ABC := 0' >> "$BUILD_DIR/Makefile.conf"
            # Build single-threaded on the 4 LITTLE cores only: slower but
            # keeps the passively-cooled radxa well under thermal limits.
            taskset -c 0,1,2,3 make -C "$BUILD_DIR" -j1
            make -C "$BUILD_DIR" install PREFIX=$HOME/.local
            rm -rf "$BUILD_DIR"
            echo "Yosys installed: $(yosys --version)"
          fi

      - name: Check and install SymbiYosys
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          if ! command -v sby &> /dev/null; then
            echo "sby not found, installing v0.68..."
            export TMPDIR="$HOME/tmp"
            mkdir -p "$TMPDIR"
            rm -rf "$HOME/symbiyosys-build"
            git clone --depth 1 --branch v0.68 https://github.com/YosysHQ/symbiyosys.git "$HOME/symbiyosys-build"
            make -C "$HOME/symbiyosys-build" install PREFIX=$HOME/.local
            rm -rf "$HOME/symbiyosys-build"
          else
            echo "sby is already installed."
          fi
          yosys --version
          z3 --version
          cvc4 --version
          sby --version

      - name: Run Formal Proofs
        env:
          COURSIER_JVM: system
          JAVA_HOME: /usr/lib/jvm/default-java
          JAVA_OPTS: "-Xmx8G"
          MAKEFLAGS: "-j 1"
        run: |
          set -e
          # $HOME/.local/bin first: the built yosys 0.33 must shadow the system
          # yosys 0.23. No venv in this job, so the sby launcher's python3 still
          # resolves to the system one (which has click/yaml).
          export PATH="$HOME/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
          echo "Starting formal proofs for all spinalML.symbolicTest specs."
          for FILE in $(find spinalML/test/src/spinalML/symbolicTest -name "*Formal.scala" | sort); do
            GRP=$(dirname "$FILE" | sed 's|spinalML/test/src/spinalML/||' | tr '/' '.')
            CLASS="spinalML.$GRP.$(basename "$FILE" .scala)"
            echo "=========================================================="
            echo "Running proof on LITTLE cores: $CLASS"
            echo "=========================================================="
            taskset -c 0,1,2,3 mill --no-server --disable-ticker spinalML.test.runMain "$CLASS"
            echo "Cooling down for 1 second..."
            sleep 1
          done