# LiteX / LiteDRAM code-generation stack (PyPI, exact pins).
# Installed into the uv-managed env (~/.spinalml_tools/.venv) by `spinalml setup`.
# migen 0.9.2 matches the version vendored by oss-cad-suite: same bits both sides.
migen==0.9.2
litex==2024.12
litedram==2024.12
