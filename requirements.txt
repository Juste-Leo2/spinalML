# SpinalML CLI runtime only (typer, rich, requests).
# This is the ONLY requirements file at the repo root, on purpose:
# full dev/test envs (pytest, cocotb, numpy, LiteX stack) are uv-managed,
# run `python cli/main.py setup` (or `setup --dev` for co-simulation).
# Advanced users (IDE, custom envs) can hand-install requirements/{base,dram,dev}.txt.
typer>=0.9.0
requests>=2.31.0
rich>=13.0.0
